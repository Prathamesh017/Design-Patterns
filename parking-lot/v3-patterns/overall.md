if (object has too many construction steps or optional params)
    → use Builder
else if (you have to decide which object to create based on some condition)
    → use Factory
else if (the object’s behavior can vary based on situation)
    → use Strategy
