let try to indentify all the entities
1) Customer 2)Driver 3)Ride

Customer
1) will first register themselves (id,name,rating)
2) will search a vechile(current address , destination) - will get all vechile types with price 
3) book a vechile(current address, destination,vechile type,price) - will select one of the vechile
4) pay -
5) cancel


Driver
1) will first register themselves (id,name,rating,vechile type)
2) accept ride
3) reject ride


Ride(ride details - price , location(current-final),state,driverid ,customerid)
1) allocate driver based on location
2) calculate price


